{
  "name": "reservoir",
  "version": "0.1.0",
  "description": "Lake's package repository for the Lean community.",
  "author": "Mac Malone",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tydeu/reservoir.git"
  },
  "workspaces": [
    "site",
    "api/v1"
  ],
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd site && npm run build",
    "dev": "cd site && npm run dev",
    "generate": "cd site && npm run generate",
    "preview": "cd site && npm run preview",
    "postinstall": "cd site && npm run postinstall"
  },
  "devDependencies": {
    "@iconify-json/eva": "^1.2.1",
    "netlify-cli": "^17.15.2"
  }
}
